
clc;
clear;

tb = input("Enter total bandwidth(MHz) : ");
cb = input("Enter two simplex channel  bandwidth(KHz) : ");
N = input("Enter number of cell : ");

cb = 2*cb;
totalAvailableChannel = tb*1000/cb;

ans = totalAvailableChannel/N;

disp("Total number of channel available per cell", ans);
